/**
 * Contiene las clases que representan una abstracción de las tablas a modo de Objeto
 * generalmente tienen la misma estructura en campos que la tabla de la cual se extraen los datos
 */
package com.amd.dbf.beans;